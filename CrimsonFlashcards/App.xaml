<?xml version="1.0" encoding="utf-8" ?>
<Application xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="CrimsonFlashcards.App">
    <Application.Resources>
        <ResourceDictionary>

            <Color x:Key="BackgroundDark">#323250</Color>
            <Color x:Key="SurfaceDark">#1c1c1c</Color>
            <Color x:Key="SidebarBackground">#1A1A1A</Color>
            <Color x:Key="ContentBackground">#181818</Color>
            <Color x:Key="TextPrimary">#FFFFFF</Color>
            <Color x:Key="TextSecondary">#B0B0B0</Color>
            <Color x:Key="CrimsonAccent">#DC304f</Color>
            <Color x:Key="CrimsonDark">#aC101d</Color>

            <SolidColorBrush x:Key="PrimaryTextBrush" Color="{StaticResource TextPrimary}" />
            <SolidColorBrush x:Key="CrimsonBrush" Color="{StaticResource CrimsonAccent}" />

            <Style x:Key="SidebarButtonStyle" TargetType="Button">
                <Setter Property="TextColor" Value="{StaticResource TextPrimary}" />
                <Setter Property="BackgroundColor" Value="Transparent" />
                <Setter Property="FontSize" Value="16" />
                <Setter Property="Padding" Value="12,10" />
                <Setter Property="Margin" Value="0" />
                <Setter Property="HorizontalOptions" Value="Fill" />
                <Setter Property="CornerRadius" Value="8" />
                <Style.Triggers>
                    <Trigger TargetType="Button" Property="IsPressed" Value="True">
                        <Setter Property="BackgroundColor" Value="{StaticResource CrimsonDark}" />
                    </Trigger>
                </Style.Triggers>
            </Style>
            
            <Style x:Key="HideButtonStyle" TargetType="Button">
                <Setter Property="TextColor" Value="{StaticResource TextSecondary}" />
                <Setter Property="BackgroundColor" Value="Transparent" />
                <Setter Property="FontSize" Value="14" />
                <Setter Property="Margin" Value="0,20,0,0" />
                <Setter Property="CornerRadius" Value="8" />
            </Style>

            <Style x:Key="HamburgerButtonStyle" TargetType="Button">
                <Setter Property="BackgroundColor" Value="Transparent" />
                <Setter Property="TextColor" Value="{StaticResource CrimsonAccent}" />
                <Setter Property="FontSize" Value="28" />
                <Setter Property="Padding" Value="10,0" />
                <Setter Property="Margin" Value="0" />
            </Style>

            <Style TargetType="Page">
                <Setter Property="BackgroundColor" Value="{StaticResource BackgroundDark}" />
            </Style>

            <Style TargetType="Label">
                <Setter Property="TextColor" Value="{StaticResource TextPrimary}" />
            </Style>

        </ResourceDictionary>
    </Application.Resources>
</Application>